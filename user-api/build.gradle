plugins {
	id 'java'
	id 'org.springframework.boot' version '3.5.5'
	id 'io.spring.dependency-management' version '1.1.7'
}

group = 'com.example'
version = '0.0.1-SNAPSHOT'

java {
	toolchain {
		languageVersion = JavaLanguageVersion.of(21)
	}
}

repositories {
	mavenCentral()
}

dependencies {
	implementation 'org.springframework.boot:spring-boot-starter-validation'
	implementation 'org.springframework.boot:spring-boot-starter-data-jpa'
	implementation 'org.springframework.boot:spring-boot-starter-web'
	implementation 'org.springframework.boot:spring-boot-starter-security'

	// Driver do PostgreSQL
	implementation 'org.postgresql:postgresql:42.7.11'
	// Flyway: engine de migração e versionamento do banco
	implementation 'org.flywaydb:flyway-core'
	// Não esquecer de incluir o conector Flyway <=> Banco específico
	implementation 'org.flywaydb:flyway-database-postgresql'

	// Para usar SQLite como banco de dados:
	// implementation 'org.xerial:sqlite-jdbc:3.46.0.0'
	// implementation 'org.hibernate.orm:hibernate-community-dialects:6.4.4.Final'

	// Para usar H2 como banco de dados em memória:
	// runtimeOnly 'com.h2database:h2'

	// Para usar mensageria RabbitMQ:
	// implementation 'org.springframework.boot:spring-boot-starter-amqp'

	testImplementation 'org.springframework.boot:spring-boot-starter-test'
	testRuntimeOnly 'org.junit.platform:junit-platform-launcher'
}

tasks.named('test') {
	useJUnitPlatform()
}
